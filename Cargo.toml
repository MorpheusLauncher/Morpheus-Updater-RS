[package]
name = "MorpheusUpdater"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
reqwest = { version = "0.12.15", features = ["blocking"] }
zip = "2.6.1"
dirs = "6.0.0"

[build-dependencies]
winresource = "0.1.20"
